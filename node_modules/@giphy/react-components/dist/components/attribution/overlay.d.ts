import { IGif } from '@giphy/js-types';
import React from 'react';
import type { GifOverlayProps } from '../types';
type Props = {
    onClick?: (gif: IGif) => void;
};
declare const AttributionOverlay: ({ gif, isHovered, onClick }: GifOverlayProps & Props) => React.JSX.Element | null;
export default AttributionOverlay;
